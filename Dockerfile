FROM node:latest

WORKDIR /server

ARG MONGO_URL
ARG DB_NAME
ARG REFRESH_TOKEN_SECRET
ARG REFRESH_TOKEN_EXPIRY
ARG ACCESS_TOKEN_SECRET
ARG ACCESS_TOKEN_EXPIRY
ARG RESET_PASSWORD_TOKEN_EXPIRY_MINS
ARG API_KEY 

COPY . .

RUN npm install 

EXPOSE 8000

CMD [ "node", "src/index.js" ]
